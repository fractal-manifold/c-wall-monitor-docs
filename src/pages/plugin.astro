---
import Layout from '../layouts/Layout.astro';
import Section from '../components/Section.astro';
---
<Layout
  title="Plugin & skills — TokenMonitor"
  description="Install the tokenmonitor MCP plugin for Claude, Codex or Antigravity — from the command line, or the desktop app for Claude and Codex — plus the four skills and MCP tools it adds."
  canonical="https://tokenmonitor.dev/plugin.html"
  page="plugin"
>
  <div class="container">
    <div class="page-head">
      <p class="eyebrow">Plugin · skills · tools</p>
      <h1>One MCP plugin.<br>Three AI ecosystems.</h1>
      <div class="rule"></div>
      <p class="lead">The <code class="inline-code">tokenmonitor</code> plugin ships through Fractal Manifold's MCP marketplace. Claude, Codex and Antigravity each install it from the command line — and for Claude and Codex, from the desktop app too. Pick your ecosystem below, then the route you prefer, and jump to the <a href="#skills" style="color:var(--color-gold-light)">skills &amp; tools reference</a> when you're set up.</p>

      <div class="cli-tabs" role="tablist" aria-label="Choose your AI ecosystem">
        <button type="button" id="clitab-claude" class="cli-tab claude active" data-cli="claude"
                role="tab" aria-selected="true" aria-controls="clipanel-claude" tabindex="0">
          <span class="swatch claude"></span>
          <span class="label">Claude Code</span>
        </button>
        <button type="button" id="clitab-codex" class="cli-tab codex" data-cli="codex"
                role="tab" aria-selected="false" aria-controls="clipanel-codex" tabindex="-1">
          <span class="swatch codex"></span>
          <span class="label">Codex CLI</span>
        </button>
        <button type="button" id="clitab-antigravity" class="cli-tab antigravity" data-cli="antigravity"
                role="tab" aria-selected="false" aria-controls="clipanel-antigravity" tabindex="-1">
          <span class="swatch antigravity"></span>
          <span class="label">Antigravity CLI</span>
        </button>
      </div>

      <!-- ── Claude Code panel ───────────────────────────────── -->
      <div class="cli-panel active" id="clipanel-claude" data-panel="claude"
           role="tabpanel" aria-labelledby="clitab-claude" tabindex="0">
        <div class="panel-head">
          <h2>Claude Code</h2>
          <span class="marketplace">via <a href="https://github.com/fractal-manifold/mcp-marketplace" target="_blank" rel="noopener">fractal-manifold/mcp-marketplace</a></span>
        </div>

        <!-- Route switcher: terminal vs desktop app -->
        <div class="route-tabs" role="tablist" aria-label="How to install on Claude">
          <button type="button" id="route-claude-terminal" class="route-tab active" data-route="terminal"
                  role="tab" aria-selected="true" aria-controls="routepanel-claude-terminal" tabindex="0">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 17l6-5-6-5"/><path d="M12 19h8"/></svg>
            Terminal
          </button>
          <button type="button" id="route-claude-desktop" class="route-tab" data-route="desktop"
                  role="tab" aria-selected="false" aria-controls="routepanel-claude-desktop" tabindex="-1">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="4" width="20" height="13" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
            Desktop app
          </button>
        </div>

        <div class="route-panel active" id="routepanel-claude-terminal" data-route-panel="terminal"
             role="tabpanel" aria-labelledby="route-claude-terminal" tabindex="0">
          <p class="route-note">Claude Code natively supports MCP marketplaces. Two commands — first you add the marketplace, then you install the plugin from it.</p>

          <h4>Install</h4>
<div class="code-block"><span class="prompt" style="color:var(--color-gold)">&gt;</span> /plugin marketplace add fractal-manifold/mcp-marketplace
<span class="prompt" style="color:var(--color-gold)">&gt;</span> /plugin install tokenmonitor@fractalmanifold-mcp-marketplace</div>

          <h4>What it adds</h4>
<div class="code-block"><span class="cmt"># Skills (you invoke these)</span>
/tokenmonitor:configure
/tokenmonitor:theme <span class="accent" style="color:var(--color-purple-light)">&lt;day|night|auto&gt;</span>
/tokenmonitor:settings <span class="accent" style="color:var(--color-purple-light)">&lt;change&gt;…</span>
/tokenmonitor:firmware

<span class="cmt"># Tools the model calls as needed — the diagnostic four,</span>
<span class="cmt"># plus control-plane tools the skills drive for you</span>
tokenmonitor_status
tokenmonitor_health
tokenmonitor_recent_logs
tokenmonitor_provision_hint</div>

          <h4>Where it lives</h4>
          <p class="route-fine">Marketplace entries land in <code class="inline-code">~/.claude/plugins/</code>. Uninstall any time with <code class="inline-code">/plugin uninstall tokenmonitor@fractalmanifold-mcp-marketplace</code>.</p>
        </div>

        <div class="route-panel" id="routepanel-claude-desktop" data-route-panel="desktop"
             role="tabpanel" aria-labelledby="route-claude-desktop" tabindex="0">
          <p class="route-note">Prefer clicking to typing? The Claude desktop app installs the very same plugin from its Settings panel — same skills, same tools, no terminal needed.</p>
          <ol class="gui-steps">
            <li>Open <strong>Settings → Plugins</strong>, then click <strong>Add ▾</strong> in the top-right and choose <strong>Add marketplace</strong>.</li>
            <li>Enter the marketplace repo <code class="inline-code">fractal-manifold/mcp-marketplace</code> and confirm.</li>
            <li>Back in the plugin list, find <strong>Tokenmonitor</strong> by <em>fractal-manifold</em> and click <strong>Install</strong>.</li>
          </ol>
          <figure class="shot">
            <img src="/assets/claude-desktop-add-marketplace.png"
                 alt="Claude desktop Settings → Plugins with the Add menu open, showing the Add marketplace option"
                 width="720" height="232" loading="lazy" decoding="async" />
            <figcaption>Claude desktop → Settings → Plugins → <strong>Add ▾</strong> → <strong>Add marketplace</strong>.</figcaption>
          </figure>
        </div>
      </div>

      <!-- ── Codex CLI panel ─────────────────────────────────── -->
      <div class="cli-panel" id="clipanel-codex" data-panel="codex"
           role="tabpanel" aria-labelledby="clitab-codex" tabindex="0">
        <div class="panel-head">
          <h2>Codex CLI</h2>
          <span class="marketplace">via <a href="https://github.com/fractal-manifold/mcp-marketplace" target="_blank" rel="noopener">fractal-manifold/mcp-marketplace</a></span>
        </div>

        <!-- Route switcher: terminal vs desktop app -->
        <div class="route-tabs" role="tablist" aria-label="How to install on Codex">
          <button type="button" id="route-codex-terminal" class="route-tab active" data-route="terminal"
                  role="tab" aria-selected="true" aria-controls="routepanel-codex-terminal" tabindex="0">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 17l6-5-6-5"/><path d="M12 19h8"/></svg>
            Terminal
          </button>
          <button type="button" id="route-codex-desktop" class="route-tab" data-route="desktop"
                  role="tab" aria-selected="false" aria-controls="routepanel-codex-desktop" tabindex="-1">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="2" y="4" width="20" height="13" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
            Desktop app
          </button>
        </div>

        <div class="route-panel active" id="routepanel-codex-terminal" data-route-panel="terminal"
             role="tabpanel" aria-labelledby="route-codex-terminal" tabindex="0">
          <p class="route-note">Codex CLI reads the same marketplace Claude Code does. Two shell commands — add the marketplace, then install the plugin from it. Nothing else to install first: the plugin bundles its own server.</p>

          <h4>Install</h4>
<div class="code-block"><span class="prompt" style="color:var(--color-gold)">$</span> codex plugin marketplace add fractal-manifold/mcp-marketplace
<span class="prompt" style="color:var(--color-gold)">$</span> codex plugin add tokenmonitor@fractalmanifold-mcp-marketplace</div>
          <p class="route-fine" style="margin-top: -0.4rem;">Skills and MCP servers are read when a thread starts, so <strong>open a new Codex thread</strong> afterwards. Confirm with <code class="inline-code">codex plugin list</code> — <code class="inline-code">tokenmonitor@fractalmanifold-mcp-marketplace</code> should read <em>installed, enabled</em>.</p>

          <h4>What it adds</h4>
<div class="code-block"><span class="cmt"># Skills (you invoke these) — the CLI gets all four</span>
/tokenmonitor:configure
/tokenmonitor:theme <span class="accent" style="color:var(--color-purple-light)">&lt;day|night|auto&gt;</span>
/tokenmonitor:settings <span class="accent" style="color:var(--color-purple-light)">&lt;change&gt;…</span>
/tokenmonitor:firmware

<span class="cmt"># Tools Codex calls as needed — the diagnostic four,</span>
<span class="cmt"># plus control-plane tools the skills drive for you</span>
tokenmonitor_status
tokenmonitor_health
tokenmonitor_recent_logs
tokenmonitor_provision_hint</div>

          <h4>Where it lives</h4>
          <p class="route-fine">The plugin is cached per-user under <code class="inline-code">~/.codex/plugins/cache/fractalmanifold-mcp-marketplace/tokenmonitor/&lt;version&gt;/</code>. Installing writes the marketplace source and an <code class="inline-code">enabled</code> flag to <code class="inline-code">~/.codex/config.toml</code> — you never hand-author an <code class="inline-code">[mcp_servers]</code> block.</p>

          <h4>Update &amp; remove</h4>
<div class="code-block"><span class="cmt"># updating takes both steps — refreshing the marketplace</span>
<span class="cmt"># snapshot alone leaves the cached plugin where it was</span>
<span class="prompt" style="color:var(--color-gold)">$</span> codex plugin marketplace upgrade fractalmanifold-mcp-marketplace
<span class="prompt" style="color:var(--color-gold)">$</span> codex plugin add tokenmonitor@fractalmanifold-mcp-marketplace

<span class="cmt"># uninstall</span>
<span class="prompt" style="color:var(--color-gold)">$</span> codex plugin remove tokenmonitor@fractalmanifold-mcp-marketplace</div>
          <p class="route-fine" style="margin-top: -0.4rem;">Either way, start a fresh thread — a running one keeps whatever it loaded at start.</p>

          <div class="route-warn">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="10"/><path d="M12 8v4M12 16h.01"/></svg>
            <div><strong>Don't register it by hand.</strong> A <code class="inline-code">codex mcp add</code> entry pointed at a <code class="inline-code">tokenmonitor-mcp</code> binary gets you neither the four skills nor the plugin manifest's 60-second first-start allowance — which the server needs the one time it resolves its runtime. Go through the marketplace.</div>
          </div>
        </div>

        <div class="route-panel" id="routepanel-codex-desktop" data-route-panel="desktop"
             role="tabpanel" aria-labelledby="route-codex-desktop" tabindex="0">
          <p class="route-note">The Codex desktop app browses the same marketplace. Same plugin, same skills and tools — clicked instead of typed.</p>
          <ol class="gui-steps">
            <li>Open <strong>Plugins</strong>, click <strong>Create ▾</strong> in the top-right and choose <strong>Add marketplace</strong>.</li>
            <li>Point it at the marketplace repo <code class="inline-code">fractal-manifold/mcp-marketplace</code>.</li>
            <li>The <code class="inline-code">fractalmanifold-mcp-marketplace</code> section appears — install the <strong>TokenMonitor</strong> plugin (&ldquo;Control and diagnose TokenMonitor&hellip;&rdquo;).</li>
          </ol>
          <figure class="shot shot-wide">
            <img src="/assets/codex-desktop-add-marketplace.png"
                 alt="Codex desktop Plugins page with the Create menu open, showing the Add marketplace option"
                 width="1325" height="365" loading="lazy" decoding="async" />
            <figcaption>Codex desktop → Plugins → <strong>Create ▾</strong> → <strong>Add marketplace</strong>.</figcaption>
          </figure>
          <figure class="shot">
            <img src="/assets/codex-desktop-tokenmonitor-installed.png"
                 alt="Codex desktop Plugins page showing the fractalmanifold-mcp-marketplace section with the TokenMonitor plugin"
                 width="932" height="752" loading="lazy" decoding="async" />
            <figcaption>The <code class="inline-code">fractalmanifold-mcp-marketplace</code> section, with the TokenMonitor plugin ready to install.</figcaption>
          </figure>
        </div>
      </div>

      <!-- ── Antigravity CLI panel ───────────────────────────── -->
      <div class="cli-panel" id="clipanel-antigravity" data-panel="antigravity"
           role="tabpanel" aria-labelledby="clitab-antigravity" tabindex="0">
        <div class="panel-head">
          <h2>Antigravity CLI</h2>
          <span class="marketplace">via <a href="https://github.com/fractal-manifold/mcp-marketplace" target="_blank" rel="noopener">fractal-manifold/mcp-marketplace</a></span>
        </div>

        <!-- Antigravity ships no desktop app, so this panel has one route only —
             deliberately no sub-tab bar rather than a disabled second segment. -->
        <div class="route-solo">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 17l6-5-6-5"/><path d="M12 19h8"/></svg>
          Terminal only
        </div>

        <p class="route-note">Antigravity — the <code class="inline-code">agy</code> CLI, Google's successor to the Gemini CLI — installs plugins straight from a GitHub subpath. One command; the plugin is self-contained (it bundles its own MCP server), so there's no separate broker install to wire up. (It still runs the Gemini-family models, e.g. <code class="inline-code">gemini-3.5-flash</code> and <code class="inline-code">gemini-3.1-pro</code>.)</p>

        <h4>Install</h4>
<div class="code-block"><span class="prompt" style="color:var(--color-gold)">$</span> agy plugin install https://github.com/fractal-manifold/mcp-marketplace/plugins/tokenmonitor</div>
        <p class="route-fine" style="margin-top: -0.4rem;">Note the subcommand is <code class="inline-code">plugin</code> (not <code class="inline-code">extensions</code>), and the target is the full <code class="inline-code">https://github.com/…</code> URL — a bare <code class="inline-code">owner/repo</code> shorthand is rejected.</p>

        <h4>Where it lands</h4>
        <span class="file-tag">~/.gemini/config/plugins/tokenmonitor/gemini-extension.json</span>
<div class="code-block">&#123;
  <span class="str">"name"</span>:    <span class="str">"tokenmonitor"</span>,
  <span class="str">"version"</span>: <span class="str">"0.11.2"</span>,
  <span class="str">"mcpServers"</span>: &#123;
    <span class="str">"tokenmonitor"</span>: &#123;
      <span class="str">"command"</span>: <span class="str">"sh"</span>,
      <span class="str">"args"</span>:    [<span class="str">"$&#123;extensionPath&#125;/server/tokenmonitor-mcp"</span>]
    &#125;
  &#125;
&#125;</div>
        <p class="route-fine" style="margin-top: -0.4rem;">The install copies the whole plugin — skills, hooks and the bundled <code class="inline-code">server/</code> launcher. Start a fresh <code class="inline-code">agy</code> session to load it; remove it any time with <code class="inline-code">agy plugin uninstall tokenmonitor</code>.</p>
      </div>

      <!-- ── Verify (common to all three) ────────────────────── -->
      <div class="verify">
        <div class="head">
          <div class="check">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12l5 5L20 7"/></svg>
          </div>
          <div>
            <h3>Verify the plugin is loaded</h3>
            <p>Same prompt works in any of the three apps. It hits <code class="inline-code">tokenmonitor_status</code> under the hood.</p>
          </div>
        </div>
        <div class="chat-bubble user">
          What's the status of my TokenMonitor broker?
        </div>
        <div class="chat-bubble cli">
          The broker is running with role <code>leader</code> on <code>localhost:8765</code>. It last served <code>tmon-3c7c9a02</code> a few seconds ago and has handled <strong style="color: var(--color-gold-light);">2,847 requests</strong> since it started 4&nbsp;hours ago. Health: <strong style="color: var(--color-success);">PASS</strong>.
        </div>
        <p style="margin-top: 1.5rem; font-size: 0.92rem; color: var(--fg-3);">If the answer comes back with a different broker role, an empty request count, or the app says it can't find the tool — head to the <a href="/faq.html" style="color: var(--color-gold-light);">troubleshooting page</a>.</p>
      </div>

      <!-- ══════════════════════════════════════════════════════ -->
      <!-- ── Skills & tools reference ────────────────────────── -->
      <!-- ══════════════════════════════════════════════════════ -->
      <div class="part-head" id="skills">
        <p class="eyebrow">Skills &amp; MCP tools</p>
        <h2>Four skills. Four core tools.</h2>
        <div class="rule"></div>
        <p class="lead">Skills are explicit commands you invoke; tools are functions the model can call on your behalf. Both are loaded the moment the <code class="inline-code">tokenmonitor</code> plugin is installed — nothing else to wire up.</p>
      </div>

      <!-- ── Skills (pulled from SKILL.md frontmatter) ────────── -->
      <div class="skills-grid">
        <article class="skill-card">
          <div class="skill-name">/tokenmonitor:configure</div>
          <h3>Pair or reconfigure a device</h3>
          <p class="desc">Provision a new device, or reconfigure an existing one, from the LAN. Discovers via mDNS, prompts for the 6-digit pairing code, pushes broker URL + auto-generated PSK, registers the device locally.</p>

          <h4>When to use</h4>
          <ul>
            <li><span class="b"></span>New device showing "Waiting for setup" / pairing code on screen</li>
            <li><span class="b"></span>Moving the broker to a different machine</li>
            <li><span class="b"></span>Rotating the passphrase / PSK</li>
            <li><span class="b"></span>Re-establishing trust after a device factory reset</li>
          </ul>

          <h4>Syntax</h4>
          <div class="syntax"><span class="arg">/tokenmonitor:configure</span></div>

          <h4>What it does, step by step</h4>
          <ul>
            <li><span class="b"></span>Scans <code class="inline-code">_tmon._tcp.local</code> for advertised devices</li>
            <li><span class="b"></span>Prompts you to confirm the pairing code on the device</li>
            <li><span class="b"></span>Generates a random per-device key (or hashes a passphrase you set)</li>
            <li><span class="b"></span>POSTs the broker URL + key to the device over the LAN</li>
            <li><span class="b"></span>Writes the device entry under <code class="inline-code">~/.config/tokenmonitor/devices/</code></li>
          </ul>
        </article>

        <article class="skill-card">
          <div class="skill-name">/tokenmonitor:theme</div>
          <h3>Switch themes remotely</h3>
          <p class="desc">Flip a device between Day, Night or Auto themes from the comfort of your CLI. Auto follows sunrise/sunset for the device's configured city with a ±90 s hysteresis.</p>

          <h4>When to use</h4>
          <ul>
            <li><span class="b"></span>Force Day mode for a screenshot</li>
            <li><span class="b"></span>Force Night mode when you're working late and the sensor lags</li>
            <li><span class="b"></span>Reset to Auto after manual overrides</li>
          </ul>

          <h4>Syntax</h4>
          <div class="syntax"><span class="arg">/tokenmonitor:theme</span> <span class="opt">&lt;day | night | auto&gt;</span> [<span class="opt">--device</span> <span class="arg">&lt;device_id&gt;</span>]</div>

          <h4>Examples</h4>
          <div class="syntax" style="margin-bottom: 0.5rem"><span class="arg">/tokenmonitor:theme</span> auto</div>
          <div class="syntax" style="margin-bottom: 0.5rem"><span class="arg">/tokenmonitor:theme</span> night <span class="opt">--device</span> tmon-3c7c9a02</div>
          <div class="syntax"><span class="arg">/tokenmonitor:theme</span> day <span class="opt">--device</span> kitchen</div>
        </article>

        <article class="skill-card">
          <div class="skill-name">/tokenmonitor:settings</div>
          <h3>Reconfigure a provisioned device</h3>
          <p class="desc">Push runtime settings to an already-paired device via the control plane: city, day / night brightness, alert volume, which providers are enabled, auto-rotation, broker URL, passphrase rotation. The change applies on the device's next poll — about 10 seconds — with automatic rollback if it can't be promoted.</p>

          <h4>When to use</h4>
          <ul>
            <li><span class="b"></span>"Set the city to Madrid"</li>
            <li><span class="b"></span>"Lower night brightness to 15"</li>
            <li><span class="b"></span>"Mute alerts" / "set volume to 0"</li>
            <li><span class="b"></span>"Disable Codex on the TokenMonitor"</li>
            <li><span class="b"></span>"Rotate providers every 60 s"</li>
            <li><span class="b"></span>"Rotate the broker passphrase"</li>
            <li><span class="b"></span>"Change the broker URL"</li>
          </ul>

          <h4>Syntax</h4>
          <div class="syntax"><span class="arg">/tokenmonitor:settings</span> <span class="opt">&lt;natural-language change&gt;</span> [<span class="opt">--device</span> <span class="arg">&lt;device_id&gt;</span>]</div>

          <h4>Out of scope</h4>
          <p class="desc" style="font-size:0.88rem">This skill will not change WiFi blind. Ask it to and it hands you to <code class="inline-code">/tokenmonitor:configure</code>, which does it properly: over a <strong>USB cable</strong> (no factory reset, and it keeps the broker URL and key), or over the LAN when the device already remembers the network you are naming. Note the cable is only listening while the device is in <strong>pairing mode</strong> — on a device that is already online, hold <strong>BOOT</strong> for 3–10 seconds and let go first; it reboots into pairing mode and then the USB route works. A device only ever auto-joins networks it has a password for, so an open network has to be chosen over the cable or on the device's own screen. Display and provider tweaks are also available in the on-device Settings panel (tap the gear on the dashboard).</p>
        </article>

        <article class="skill-card">
          <div class="skill-name">/tokenmonitor:firmware</div>
          <h3>Stage a signed firmware update</h3>
          <p class="desc">Stage an over-the-air firmware update to the device across your LAN, through the broker. Each image is an Ed25519-signed manifest the device verifies before the boot slot switches — with automatic rollback if the new build doesn't check in healthy, and an anti-rollback floor against downgrades.</p>

          <h4>When to use</h4>
          <ul>
            <li><span class="b"></span>A new firmware release is available for your device</li>
            <li><span class="b"></span>You want to move a device onto the dev / canary channel</li>
            <li><span class="b"></span>Re-applying after an automatic rollback</li>
          </ul>

          <h4>Syntax</h4>
          <div class="syntax"><span class="arg">/tokenmonitor:firmware</span> [<span class="opt">--device</span> <span class="arg">&lt;device_id&gt;</span>]</div>

          <h4>Safe by design</h4>
          <p class="desc" style="font-size:0.88rem">Updates only ever flow over your own LAN, and the device installs nothing it can't cryptographically verify. If a build won't boot cleanly, it reverts itself.</p>
        </article>
      </div>

      <!-- ── 4 MCP tools (table-style) ────────────────────────── -->
      <div class="tools-section">
        <p class="eyebrow">MCP tools</p>
        <h2>What the model can call directly.</h2>
        <div class="rule"></div>
        <p class="lead" style="max-width: 64ch;">These are the four you'll reach for directly — your AI agent calls them in response to natural-language questions, no slash-skill needed. The skills drive a larger set of control-plane tools (device registry, discovery, firmware publish / revert) behind the scenes.</p>

        <div class="tools-grid">
          <div class="tool-card">
            <div class="tool-name">tokenmonitor_status</div>
            <h4>Runtime, role + request count</h4>
            <p>Returns the broker's runtime, its role (<code class="inline-code">leader</code> or <code class="inline-code">follower</code>), when it last served the device, and the total requests handled since startup.</p>
            <div class="returns">
              <span>runtime</span><span>role: leader|follower</span><span>last_request_at</span><span>requests_total</span>
            </div>
          </div>
          <div class="tool-card">
            <div class="tool-name">tokenmonitor_health</div>
            <h4>PASS / FAIL diagnostic per check</h4>
            <p>Runs a PASS/FAIL check across your provider credentials (present, parseable, not expired), a signed self-ping to the broker, and recently observed device traffic.</p>
            <div class="returns">
              <span>ok</span><span>credentials</span><span>self_ping</span><span>observed_traffic</span>
            </div>
          </div>
          <div class="tool-card">
            <div class="tool-name">tokenmonitor_recent_logs</div>
            <h4>Tail the broker log</h4>
            <p>Returns the last N lines (default 50) of the broker's structured log. Useful when a device shows stale data and you want to see whether the broker even received the poll.</p>
            <div class="returns">
              <span>limit: string</span>
            </div>
          </div>
          <div class="tool-card">
            <div class="tool-name">tokenmonitor_provision_hint</div>
            <h4>Suggests laptop LAN URLs</h4>
            <p>Lists candidate broker URLs (your laptop's LAN interfaces) ranked by reachability. The captive portal uses this to populate the broker-URL field when you'd rather not run the configure skill.</p>
            <div class="returns">
              <span>urls[]</span><span>preferred: string</span>
            </div>
          </div>
        </div>
      </div>

      <!-- ── Example end-to-end flow ──────────────────────────── -->
      <div class="flow">
        <p class="eyebrow" style="color: var(--color-purple-light);">Example</p>
        <h3>A new device, end to end.</h3>
        <p class="lead-sm">Powered on, pairing code visible, plugin installed. From here:</p>
        <div class="flow-row">
          <div>
            <div class="chat-bubble user">I just powered on a new TokenMonitor. It's showing pairing code <code>428 715</code>.</div>
            <div class="chat-bubble cli">I'll configure it. Running <code>/tokenmonitor:configure</code>… Found <code>tmon-3c7c9a02</code> at <code>192.168.1.42</code> via mDNS. Generated a device key and pushed it over the LAN. It's rebooting now — give it ~10 s.</div>
          </div>
          <div>
            <div class="chat-bubble user">Is my broker actually serving it?</div>
            <div class="chat-bubble cli">Called <code>tokenmonitor_status</code>. Broker is running with role <code>leader</code>; it last served <code>tmon-3c7c9a02</code> seconds ago, <strong style="color: var(--color-gold-light);">12 requests</strong> so far. Health <strong style="color: var(--color-success);">PASS</strong>.</div>
            <div class="chat-bubble user">Switch to night mode.</div>
            <div class="chat-bubble cli">Ran <code>/tokenmonitor:theme night</code>. Device acknowledged in 110 ms.</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ── CTA ────────────────────────────────────────────────── -->
  <Section spacing="flush">
    <div class="close-cta" style="text-align:center; padding: 4rem 2rem; background: var(--color-bg-tertiary); border: var(--border-faint); border-radius: var(--radius-xl); backdrop-filter: blur(8px); margin-top: 4rem;">
      <p class="eyebrow">Next step</p>
      <h2 style="margin-bottom: 1rem">Configure your device.</h2>
      <div class="rule" style="margin: 1.5rem auto;"></div>
      <p style="max-width: 56ch; margin: 0 auto 2rem auto;">With the plugin loaded, <code class="inline-code">/tokenmonitor:configure</code> takes over from here. It discovers the device on your LAN, prompts for the pairing code, pushes the PSK.</p>
      <a href="/setup.html" class="btn btn-primary">Full setup walkthrough <span class="arrow">→</span></a>
    </div>
  </Section>
</Layout>

<script>
  // Two levels of tabs: which CLI (top) and which route within it (terminal /
  // desktop app). Both are wired by the same helper; the route lists are scoped
  // to their own panel so the two levels never steal each other's buttons.
  type TabGroup = { tabs: HTMLElement[]; select: (tab: HTMLElement) => void };

  // Assigned once both groups exist; called after any *user-driven* selection
  // (mouse or keyboard) so the URL tracks the UI either way.
  let syncHash = () => {};

  function wire(tabs: HTMLElement[], panels: HTMLElement[], key: 'cli' | 'route'): TabGroup {
    const panelKey = key === 'cli' ? 'panel' : 'routePanel';
    const select = (tab: HTMLElement) => {
      const value = tab.dataset[key];
      tabs.forEach(t => {
        const on = t === tab;
        t.classList.toggle('active', on);
        t.setAttribute('aria-selected', String(on));
        t.tabIndex = on ? 0 : -1;
      });
      panels.forEach(p => p.classList.toggle('active', p.dataset[panelKey] === value));
    };
    tabs.forEach((tab, i) => {
      tab.addEventListener('click', () => { select(tab); syncHash(); });
      tab.addEventListener('keydown', (e: KeyboardEvent) => {
        const step = e.key === 'ArrowRight' ? 1 : e.key === 'ArrowLeft' ? -1 : 0;
        if (!step) return;
        e.preventDefault();
        const next = tabs[(i + step + tabs.length) % tabs.length];
        select(next);
        next.focus();
        syncHash();
      });
    });
    return { tabs, select };
  }

  const cliTabs = [...document.querySelectorAll<HTMLElement>('.cli-tabs .cli-tab')];
  const cliGroup = wire(cliTabs, [...document.querySelectorAll<HTMLElement>('.cli-panel')], 'cli');

  // One route group per CLI panel. Antigravity has no desktop route and so no
  // .route-tabs element — it simply yields no group.
  const routeGroups = new Map<string, TabGroup>();
  document.querySelectorAll<HTMLElement>('.cli-panel').forEach(panel => {
    const bar = panel.querySelector('.route-tabs');
    if (!bar) return;
    const group = wire(
      [...bar.querySelectorAll<HTMLElement>('.route-tab')],
      [...panel.querySelectorAll<HTMLElement>('.route-panel')],
      'route',
    );
    routeGroups.set(panel.dataset.panel!, group);
  });

  // Keep the URL shareable. replaceState doesn't fire hashchange, so this can't
  // loop back into applyHash().
  syncHash = () => {
    const cli = cliTabs.find(t => t.classList.contains('active'))?.dataset.cli;
    if (!cli) return;
    const route = routeGroups.get(cli)?.tabs.find(t => t.classList.contains('active'))?.dataset.route;
    history.replaceState(null, '', `#${cli}${route && route !== 'terminal' ? `-${route}` : ''}`);
  };

  // Deep links: #codex opens that CLI on its default (terminal) route,
  // #codex-desktop on the desktop-app route. Any other hash — notably the
  // page's own #skills anchor — is left entirely alone.
  const applyHash = () => {
    const hash = location.hash.replace(/^#/, '');
    if (!hash) return;
    const [cli, route] = hash.split('-');
    const cliTab = cliTabs.find(t => t.dataset.cli === cli);
    if (!cliTab) return;
    cliGroup.select(cliTab);
    const group = routeGroups.get(cli);
    if (group) {
      // No route named, or one this CLI doesn't have (#antigravity-desktop):
      // fall back to the first route rather than leaving whatever was open.
      const routeTab = route && group.tabs.find(t => t.dataset.route === route);
      group.select(routeTab || group.tabs[0]);
    }
    syncHash();  // normalises an unsupported route back out of the URL
  };
  applyHash();
  window.addEventListener('hashchange', applyHash);
</script>

<style is:global>
  .page-head {
    padding: 4rem 0 3rem 0;
  }
  .page-head h1 {
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    letter-spacing: 1px;
    line-height: 1.1;
    margin-bottom: 1.25rem;
  }
  .page-head .lead { max-width: 62ch; font-size: 1.18rem; }

  /* ── Section divider heading (between install + reference) ── */
  .part-head {
    margin-top: 5rem;
    padding-top: 3.5rem;
    border-top: var(--border-faint);
  }
  .part-head h2 {
    font-size: clamp(1.8rem, 4vw, 2.6rem);
    letter-spacing: 0.5px;
    margin: 0.5rem 0 0 0;
  }
  .part-head .lead { max-width: 60ch; font-size: 1.08rem; margin-top: 1rem; }

  /* ── CLI switcher — one segmented control, not three cards ─
     The card treatment this replaced (3-col grid, hover lift, faint active
     border) read as three separate nav cards. A single bordered track with
     equal segments plus a caret into the panel makes it obvious that these
     swap the content below. */
  .cli-tabs {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 4px;
    padding: 5px;
    margin: 3rem 0 0 0;
    background: var(--color-bg-tertiary);
    border: var(--border-faint);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(8px);
  }
  .cli-tab {
    position: relative;
    background: transparent;
    border: 1px solid transparent;
    border-radius: var(--radius-md);
    padding: 0.9rem 1rem;
    cursor: pointer;
    transition: background var(--ease-snappy), color var(--ease-snappy), border-color var(--ease-snappy);
    display: flex; align-items: center; justify-content: center; gap: 0.6rem;
    font-family: var(--font-display);
    font-size: 0.82rem;
    letter-spacing: 1px;
    text-transform: uppercase;
    color: var(--fg-3);
    white-space: nowrap;
  }
  .cli-tab:hover { background: rgba(255,255,255,0.04); color: var(--fg-1); }
  .cli-tab:focus-visible { outline: 2px solid var(--color-gold); outline-offset: 2px; }
  .cli-tab .swatch {
    width: 10px; height: 10px; border-radius: 50%;
    flex-shrink: 0;
    opacity: 0.45;
    transition: opacity var(--ease-snappy), box-shadow var(--ease-snappy);
  }
  .cli-tab .swatch.claude { background: #e07a4a; }
  .cli-tab .swatch.codex  { background: #1ed29c; }
  .cli-tab .swatch.antigravity { background: #3186FF; }
  .cli-tab.active .swatch { opacity: 1; }
  .cli-tab.active.claude .swatch { box-shadow: 0 0 12px rgba(224,122,74,0.7); }
  .cli-tab.active.codex  .swatch { box-shadow: 0 0 12px rgba(30,210,156,0.7); }
  .cli-tab.active.antigravity .swatch { box-shadow: 0 0 12px rgba(49,134,255,0.7); }

  /* Active segment: filled + tinted in the provider's own colour. */
  .cli-tab.active.claude      { background: rgba(224,122,74,0.13); border-color: rgba(224,122,74,0.40); color: #f0a077; }
  .cli-tab.active.codex       { background: rgba(30,210,156,0.13);  border-color: rgba(30,210,156,0.40);  color: #5fe0bb; }
  .cli-tab.active.antigravity { background: rgba(49,134,255,0.13);  border-color: rgba(49,134,255,0.40);  color: #7db0ff; }

  /* On cream, the light brand tones used above wash out — darken the label and
     deepen the fill so the active segment still carries the contrast. */
  [data-theme="light"] .cli-tab.active.claude      { background: rgba(224,122,74,0.16); border-color: rgba(180,84,40,0.45); color: #a5501f; }
  [data-theme="light"] .cli-tab.active.codex       { background: rgba(16,163,127,0.14); border-color: rgba(13,120,94,0.45);  color: #0b6b53; }
  [data-theme="light"] .cli-tab.active.antigravity { background: rgba(49,134,255,0.13); border-color: rgba(30,98,196,0.45);  color: #1a5cba; }
  [data-theme="light"] .cli-tab .swatch.codex { background: #10a37f; }
  [data-theme="light"] .cli-tab.active.codex .swatch { box-shadow: 0 0 12px rgba(16,163,127,0.5); }
  [data-theme="light"] .cli-tab:hover { background: rgba(20,17,11,0.05); }

  /* Caret dropping out of the active segment into the panel below. */
  .cli-tab.active::after {
    content: '';
    position: absolute;
    left: 50%; bottom: -11px;
    transform: translateX(-50%);
    border-left: 7px solid transparent;
    border-right: 7px solid transparent;
    border-top: 7px solid currentColor;
    opacity: 0.55;
  }

  /* ── CLI panel ──────────────────────────────────────────── */
  .cli-panel { display: none; }
  /* Clears the caret hanging out of the active segment above. */
  .cli-panel.active { display: block; margin-top: 2.5rem; }
  .cli-panel:focus-visible { outline: none; }
  .cli-panel .panel-head {
    display: flex; align-items: baseline; justify-content: space-between;
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: var(--border-faint);
  }
  .cli-panel h2 {
    font-size: 1.6rem;
    margin: 0;
  }
  .cli-panel .marketplace {
    font-family: var(--font-mono);
    font-size: 0.82rem;
    color: var(--fg-3);
  }
  .cli-panel .marketplace a {
    color: var(--color-gold-light);
    text-decoration: underline;
    text-decoration-color: rgba(245,158,11,0.4);
  }
  .cli-panel h4 {
    font-family: var(--font-display);
    font-size: 0.7rem;
    letter-spacing: 1.8px;
    color: var(--color-gold);
    text-transform: uppercase;
    margin: 2rem 0 0.8rem 0;
  }
  /* These blocks are authored with real newlines; without this they collapse
     into one run-on line (setup.astro carries the same rule). */
  .code-block { white-space: pre-wrap; overflow-wrap: anywhere; }
  .cli-panel .code-block { margin-bottom: 1rem; }

  .file-tag {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.82rem;
    color: var(--fg-3);
    background: rgba(255,255,255,0.04);
    padding: 4px 10px;
    border-radius: 6px;
    border: 1px solid rgba(255,255,255,0.06);
    margin-bottom: 0.6rem;
  }

  /* ── Route switcher (Terminal vs Desktop app) ─────────────
     Second level, one per CLI panel. Deliberately quieter than .cli-tabs — a
     smaller track and no brand tint — so the hierarchy reads at a glance.
     Previously these two routes ran together down one column separated only by
     a rule, and readers scrolled straight past the boundary. */
  .route-tabs {
    display: inline-flex;
    gap: 4px;
    padding: 4px;
    margin: 0 0 1.5rem 0;
    background: rgba(255,255,255,0.03);
    border: var(--border-faint);
    border-radius: var(--radius-md);
  }
  .route-tab {
    display: inline-flex; align-items: center; gap: 0.5rem;
    font-family: var(--font-display);
    font-size: 0.72rem;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    padding: 0.5rem 0.95rem;
    border: none;
    border-radius: 6px;
    background: transparent;
    color: var(--fg-3);
    cursor: pointer;
    white-space: nowrap;
    transition: all var(--ease-snappy);
  }
  .route-tab svg { opacity: 0.7; }
  .route-tab:hover { color: var(--fg-1); background: rgba(255,255,255,0.04); }
  .route-tab:focus-visible { outline: 2px solid var(--color-gold); outline-offset: 2px; }
  .route-tab.active {
    background: rgba(245,158,11,0.12);
    color: var(--color-gold);
    box-shadow: inset 0 0 0 1px rgba(245,158,11,0.25);
  }
  .route-tab.active svg { opacity: 1; }

  /* Antigravity ships no desktop app — a static label, not a dead tab. */
  .route-solo {
    display: inline-flex; align-items: center; gap: 0.5rem;
    font-family: var(--font-display);
    font-size: 0.72rem;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    padding: 0.5rem 0.95rem;
    margin: 0 0 1.5rem 0;
    border: var(--border-faint);
    border-radius: var(--radius-md);
    color: var(--fg-3);
  }

  .route-panel { display: none; }
  .route-panel.active { display: block; }
  .route-note { font-size: 1.02rem; color: var(--fg-2); line-height: 1.7; }
  .route-fine { font-size: 0.92rem; color: var(--fg-3); line-height: 1.65; }
  .route-panel > h4:first-child,
  .route-note:first-child { margin-top: 0; }

  /* Inline warning — used for "don't hand-roll a codex mcp add entry". */
  .route-warn {
    display: flex; gap: 0.85rem;
    margin-top: 2rem;
    padding: 1rem 1.15rem;
    background: rgba(245,158,11,0.05);
    border: 1px solid rgba(245,158,11,0.20);
    border-radius: var(--radius-md);
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--fg-2);
  }
  .route-warn svg { flex-shrink: 0; margin-top: 0.15rem; color: var(--color-gold); }
  .route-warn strong { color: var(--fg-1); }

  [data-theme="light"] .route-tabs { background: rgba(20,17,11,0.03); }
  [data-theme="light"] .route-tab:hover { background: rgba(20,17,11,0.05); }
  [data-theme="light"] .route-tab.active {
    background: rgba(196,112,18,0.12);
    box-shadow: inset 0 0 0 1px rgba(196,112,18,0.32);
  }
  [data-theme="light"] .route-warn {
    background: rgba(196,112,18,0.07);
    border-color: rgba(196,112,18,0.28);
  }

  /* ── GUI step list ──────────────────────────────────────── */
  .gui-steps {
    list-style: none;
    counter-reset: gui;
    padding: 0;
    margin: 0 0 1.5rem 0;
  }
  .gui-steps li {
    counter-increment: gui;
    position: relative;
    padding: 0.55rem 0 0.55rem 2.4rem;
    font-size: 0.96rem;
    line-height: 1.6;
    color: var(--fg-2);
    border-bottom: var(--border-faint);
  }
  .gui-steps li:last-child { border-bottom: none; }
  .gui-steps li::before {
    content: counter(gui);
    position: absolute;
    left: 0; top: 0.5rem;
    width: 1.55rem; height: 1.55rem;
    display: grid; place-items: center;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--color-gold-light);
    background: rgba(245,158,11,0.08);
    border: 1px solid rgba(245,158,11,0.25);
    border-radius: 50%;
  }
  .gui-steps strong { color: var(--fg-1); }

  /* ── Screenshot figures ─────────────────────────────────── */
  .shot {
    margin: 0 0 1.5rem 0;
  }
  .shot img {
    display: block;
    width: 100%;
    max-width: 560px;
    height: auto;
    border: var(--border-faint);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    background: #07070a;
  }
  /* Wide, shallow menu capture — don't blow it up to full column width. */
  .shot.shot-wide img { max-width: 680px; }
  .shot figcaption {
    margin-top: 0.6rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--fg-3);
    line-height: 1.5;
  }
  .shot figcaption strong { color: var(--fg-2); font-weight: 400; }

  /* ── Verify section ─────────────────────────────────────── */
  .verify {
    margin-top: 4rem;
    background: linear-gradient(135deg, rgba(16,185,129,0.06) 0%, rgba(245,158,11,0.05) 100%);
    border: 1px solid rgba(16,185,129,0.18);
    border-radius: var(--radius-xl);
    padding: 2.5rem;
  }
  .verify .head {
    display: flex; align-items: center; gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .verify .head .check {
    width: 44px; height: 44px;
    border-radius: 50%;
    background: rgba(16,185,129,0.15);
    border: 1px solid rgba(16,185,129,0.4);
    display: grid; place-items: center;
    color: var(--color-success);
    flex-shrink: 0;
  }
  .verify h3 {
    font-family: var(--font-display);
    font-size: 1.15rem;
    letter-spacing: 0.5px;
    margin: 0;
  }
  .verify .head p { margin: 0.2rem 0 0 0; font-size: 0.88rem; color: var(--fg-3); }
  .verify p { color: var(--fg-2); }

  /* ── Skills grid ────────────────────────────────────────── */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    margin: 3rem 0;
  }
  .skill-card {
    background: var(--color-bg-tertiary);
    border: var(--border-faint);
    border-radius: var(--radius-xl);
    padding: 2rem;
    backdrop-filter: blur(8px);
    transition: all var(--ease-standard);
    position: relative;
    overflow: hidden;
  }
  .skill-card:hover {
    transform: translateY(-3px);
    border-color: rgba(245,158,11,0.30);
    box-shadow: var(--shadow-sm);
  }
  .skill-card::after {
    content: '';
    position: absolute;
    top: -20%; right: -20%;
    width: 200px; height: 200px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(245,158,11,0.08) 0%, transparent 60%);
    pointer-events: none;
  }
  .skill-card .skill-name {
    font-family: var(--font-mono);
    font-size: 1.05rem;
    color: var(--color-gold-light);
    background: rgba(245,158,11,0.08);
    border: 1px solid rgba(245,158,11,0.2);
    padding: 0.4rem 0.85rem;
    border-radius: var(--radius-md);
    display: inline-block;
    margin-bottom: 1.25rem;
    font-variant-numeric: tabular-nums;
  }
  .skill-card h3 {
    font-family: var(--font-display);
    font-size: 1.1rem;
    letter-spacing: 0.5px;
    margin: 0 0 1rem 0;
  }
  .skill-card .desc {
    color: var(--fg-2);
    font-size: 0.96rem;
    line-height: 1.65;
  }
  .skill-card h4 {
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 1.8px;
    text-transform: uppercase;
    color: var(--color-gold);
    margin: 1.75rem 0 0.6rem 0;
  }
  .skill-card .syntax {
    font-family: var(--font-mono);
    font-size: 0.86rem;
    color: var(--fg-2);
    background: #07070a;
    border: var(--border-faint);
    border-radius: var(--radius-md);
    padding: 0.85rem 1rem;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }
  .skill-card .syntax .opt { color: var(--color-purple-light); }
  .skill-card .syntax .arg { color: var(--color-gold-light); }
  .skill-card ul {
    list-style: none;
    padding: 0; margin: 0;
  }
  .skill-card ul li {
    padding: 0.5rem 0 0.5rem 1.2rem;
    border-bottom: var(--border-faint);
    position: relative;
    font-size: 0.92rem;
    color: var(--fg-2);
  }
  .skill-card ul li:last-child { border-bottom: none; }
  /* Dot sits in the left gutter so the text + inline code flow as one run
     (a flex li would split the <code> into its own rigid column). */
  .skill-card ul li .b {
    position: absolute;
    left: 0; top: 0.95rem;
    width: 4px; height: 4px;
    border-radius: 50%;
    background: var(--color-gold);
  }

  /* ── Tool cards (the 4 MCP tools) ───────────────────────── */
  .tools-section {
    margin-top: 4rem;
  }
  .tools-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-top: 2rem;
  }
  .tool-card {
    background: var(--color-bg-tertiary);
    border: var(--border-faint);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    backdrop-filter: blur(8px);
    transition: all var(--ease-snappy);
  }
  .tool-card:hover {
    border-color: rgba(245,158,11,0.25);
    transform: translateY(-2px);
  }
  .tool-card .tool-name {
    font-family: var(--font-mono);
    font-size: 0.92rem;
    color: var(--color-gold-light);
    margin-bottom: 0.6rem;
    font-variant-numeric: tabular-nums;
  }
  .tool-card h4 {
    font-family: var(--font-display);
    font-size: 0.92rem;
    letter-spacing: 0.4px;
    color: var(--fg-1);
    margin: 0 0 0.6rem 0;
  }
  .tool-card p {
    margin: 0 0 0.8rem 0;
    font-size: 0.9rem;
    color: var(--fg-2);
    line-height: 1.55;
  }
  .tool-card .returns {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-top: 0.6rem;
  }
  .tool-card .returns span {
    font-family: var(--font-mono);
    font-size: 0.74rem;
    color: var(--fg-3);
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    padding: 2px 8px;
    border-radius: 4px;
  }

  /* ── Example flow ───────────────────────────────────────── */
  .flow {
    margin-top: 4rem;
    padding: 3rem;
    background: linear-gradient(135deg, rgba(147,51,234,0.05) 0%, rgba(245,158,11,0.04) 100%);
    border: 1px solid rgba(147,51,234,0.18);
    border-radius: var(--radius-xl);
  }
  .flow h3 {
    font-size: 1.3rem;
    letter-spacing: 0.5px;
    margin: 0 0 0.4rem 0;
  }
  .flow .lead-sm { color: var(--fg-2); margin-bottom: 2rem; }
  .flow-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
  }

  /* ── Chat bubbles (shared by verify + flow) ─────────────── */
  .chat-bubble {
    background: rgba(20,20,20,0.6);
    border: var(--border-faint);
    border-radius: var(--radius-lg);
    padding: 1rem 1.25rem;
    margin: 1rem 0;
    font-size: 0.95rem;
    line-height: 1.55;
  }
  .chat-bubble.user {
    border-left: 3px solid var(--color-gold);
    color: var(--fg-1);
  }
  .chat-bubble.user::before {
    content: 'YOU';
    display: block;
    font-family: var(--font-display);
    font-size: 0.62rem;
    letter-spacing: 1.5px;
    color: var(--color-gold);
    margin-bottom: 0.3rem;
  }
  .chat-bubble.cli {
    border-left: 3px solid var(--color-purple-light);
  }
  .chat-bubble.cli::before {
    content: 'APP';
    display: block;
    font-family: var(--font-display);
    font-size: 0.62rem;
    letter-spacing: 1.5px;
    color: var(--color-purple-light);
    margin-bottom: 0.3rem;
  }
  .chat-bubble code {
    font-family: var(--font-mono);
    background: rgba(245,158,11,0.10);
    color: var(--color-gold-light);
    padding: 1px 6px;
    border-radius: 4px;
    font-size: 0.86rem;
  }
  /* tighter bubbles inside the example flow */
  .flow .chat-bubble { padding: 0.9rem 1.1rem; margin: 0 0 1.25rem 0; border-radius: var(--radius-md); font-size: 0.92rem; }
  .flow .chat-bubble:last-child { margin-bottom: 0; }
  .flow .chat-bubble.cli::before { content: 'CLAUDE'; }

  @media (max-width: 900px) {
    /* Stays a row — stacking it back into full-width blocks is exactly what
       made it read as three cards instead of one control. */
    /* Labels wrap inside their own third rather than overflowing the track —
       "ANTIGRAVITY CLI" does not fit one line on a phone. */
    .cli-tab {
      flex-direction: column;
      gap: 0.4rem;
      padding: 0.7rem 0.35rem;
      font-size: 0.64rem;
      letter-spacing: 0.3px;
      white-space: normal;
      line-height: 1.3;
      text-align: center;
    }
    .cli-tab .swatch { width: 8px; height: 8px; }
    /* The heading and the marketplace note fight for one line once the h2 wraps. */
    .cli-panel .panel-head { flex-direction: column; align-items: flex-start; gap: 0.5rem; }
    .skills-grid { grid-template-columns: 1fr; }
    .tools-grid { grid-template-columns: 1fr; }
    .flow-row { grid-template-columns: 1fr; }
    .flow { padding: 1.75rem; }
  }
</style>
